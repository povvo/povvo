# Palette Architecture

## Palette Architecture

## Palette Identity

Black/off-white polarity with muted cyan linework and tightly scoped warm grain.

## Palette Separation

| Territory | Owned colours | Boundary rule | Role |
| --- | --- | --- | --- |
| field | off-white, grain, cyan line | dominant in light mode | inspection |
| board | near-black, pale inverse mark | identity only | formal anchor |
| scan | deep teal edge, cyan glow | edge only | optical reveal |
| construction | pale cyan outline, ink nodes | subordinate to identity | geometric proof |

## Convergence Behaviour

Territories stay separate; do not average into grey or blue.

## Container Principle

Fields expose, boards absorb, scan edges frame, and construction lines prove geometry.

## Light Quality

Frontal field light with restrained scan-edge falloff only.

## Mode Adaptation

Light field is default; black-board is emphasis; high contrast uses platform colors and structure.

## Token Translation

| Role | Token | Value | Usage |
| --- | --- | --- | --- |
| Off-white inspection field | `color.field.paper` | `#F4F6F1` | dominant light field |
| Soft paper grain | `color.field.grain` | `#DCE4DF` | subtle texture and pale diagram fill |
| Black anchor ink | `color.ink.anchor` | `#050706` | display anchors, slashes, board field |
| Muted cyan line | `color.line.cyan` | `#6E9DB2` | hairlines, focus, scan, ticks |
| Pale inverse mark | `color.mark.inverse` | `#D8ECF8` | marks on black board |
| Deep teal edge | `color.edge.teal` | `#0B2B34` | scan falloff at the outer edge only |
| Warm halftone grain | `color.texture.warm` | `#B5A293` | crop texture only |
| Error repair | `color.status.error` | `#B24A32` | repair cue with text and shape |
| Success repair | `color.status.success` | `#287F76` | success cue with text and shape |

Povvo Spec-Scan is a measured graphic language centered on one canonical identity system: the interrupted-aperture P integrated into the POVVO wordmark and detached as the standalone mark. Warm off-white and ink black carry the field; pale cyan exposes outline construction, ruler ticks, registration marks, vector nodes, and scan edges. The wordmark and P use matched filled and outline states. No unrelated symbol or modular shape may replace the P. Active silence, low print grain, and controlled optical falloff complete the language. People, sport subject matter, product imagery, and generic futuristic interfaces are excluded.

Operational terms covered here: Palette Identity, Palette Separation, Light Quality. These terms are used as build rules, not placeholders. Every artifact should be reconstructable without reopening proof material: choose a measured field or black board, place the integrated POVVO wordmark or standalone interrupted-aperture P as the identity anchor, expose its matched outline construction only where useful, and add only measured marks needed for alignment or state.

## Rules

- Black and off-white carry the system's visible weight.
- Muted cyan identifies instrument lines, focus brackets, scan falloff, and rare accents.
- Dense information appears as a bounded burst, then returns to a quiet field.
- Display type can crop and compress; body and state text stay readable.
- Source subjects are not reused; the language is standalone.

## Drift Controls

Fidelity check: preserve the interrupted-aperture P, identical integrated and standalone geometry, matched filled and outline POVVO states, black and off-white dominance, pale cyan hairlines, tiny measured marks, active negative space, scan or bracket state logic, and exact text control. Reject alternate logo geometry, unrelated symbols, modular blobs, saturated cyan panels, glossy control-room styling, generic cards, product or lifestyle imagery, people-led subjects, sport content, copied source marks, and extra generated words.

